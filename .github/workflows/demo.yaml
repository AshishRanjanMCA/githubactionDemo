name: Hello World
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build Step
        run: echo "A demo build"

  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: test phase
        run: echo "Running Test phase"

  delopy:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Deploye the app
        run: echo "Deploying the app"

        
